"""Pinned versions and labels for reproducibility (classification prompt drift audits)."""

CLASSIFIER_PROMPT_VERSION = "human-listener-orient-v3-5axis-2026-03-27"

# Squared-distance weight for (w,v) relative to (x,y,z) in neighborhood / weave / decider splits.
EXTRA_AXIS_DIST_WEIGHT = 0.85
